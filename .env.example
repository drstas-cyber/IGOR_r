# BabyLoveGrowth blog integration — build-time only, never shipped to the browser.
# Generate at: BabyLoveGrowth → Settings → Integrations → Developer → API keys.
BABYLOVE_API_KEY=your_babylovegrowth_api_key_here
